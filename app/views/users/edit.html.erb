<div class='container'>
  <div class="row">
    <div class="col-12">
      <h2 class="text-center my-5">プロフィール編集</h2>
      <%= form_with model: @user, local: true do |f| %>
        <div class="form-group row">
          <%= f.label :"ユーザー画像", class: "col-3 offset-2 h5"%>
          <%= f.file_field :image %>
        </div>
        <div class="field form-group row mt-5">
          <%= f.label :"ユーザー名", class: "col-3 offset-2 h5" %>
          <%= f.text_field :name, class: "form-control col-4" %>
        </div>
        <div class="field form-group row mt-5">
          <%= f.label :"メールアドレス", class: "col-3 offset-2 h5" %>
          <%= f.email_field :email, class: "form-control col-4" %>
        </div>
        <div class="row">
          <%= f.submit "変更",class: 'btn btn-primary offset-5 mt-3 px-4' %>
        </div>
      <% end %>
    </div>
  </div>
</div>

