<% if article.user.image.present? %>
  <%= image_tag asset_path(article.user.image.url), class: "border border-secondary rounded", :size => "70x70" %>
<% else %>
  <%= image_tag "no_image_user.png", class: "border border-secondary rounded", :size => "70x70" %>
<% end %>